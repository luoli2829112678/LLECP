#pragma once
#include<string>
using namespace std;
class FunctionManager
{
private:
    /* data */
public:
    int GetFunctionAddr(string sCmd);
    FunctionManager(/* args */);
    ~FunctionManager();
};
